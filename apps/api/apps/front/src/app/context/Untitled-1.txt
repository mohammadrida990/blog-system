      <div
      // className="
      //   flex flex-col md:flex-row
      //   gap-2 ml-auto [&>a]:py-2 [&>a]:px-4 [&>a]:transition [&>a]:rounded-md
      //   [&>a:hover]:text-sky-100 [&>a:hover]:bg-sky-500
      // "
      >
        <ThemeToggle />
        {/* 
        {session && session.user ? (
          <Profile user={session.user} />
        ) : (
          <SignInPanel />
        )} */}
      </div>